import { ColumnHostDirective } from './column-host.directive';

describe('ColumnHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnHostDirective();
    expect(directive).toBeTruthy();
  });
});
